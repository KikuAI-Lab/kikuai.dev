name: Site Deploy (Cloudflare Pages)

# Этот workflow больше не нужен - Cloudflare Pages деплоит автоматически
# Оставлен для справки или можно удалить

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "ℹ️  Сайт теперь деплоится автоматически через Cloudflare Pages"
          echo "📦 При push в main branch Cloudflare Pages автоматически:"
          echo "   1. Запускает билд: cd kikuai-site-nuxt && npm ci && npm run build"
          echo "   2. Деплоит в production"
          echo "   3. Обновляет kikuai.dev"
          echo ""
          echo "🔗 Проверь деплой: https://dash.cloudflare.com → Pages → kikuai-site"

